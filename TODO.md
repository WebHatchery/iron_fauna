# TODO — IRON FAUNA

## Content

- Only one quest is authored (`morning_thinning`) and `QuestObjectiveKind` has a single variant, `SubdueWild`. The five outer regions ship settlements, maps, and factories but no bounties.
- Species roster stands at 32 against the launch target of 30+ (`creature.md` §9); graftware, elements, and chassis coverage should be checked against that roster as species are added.

## Balance

- Power Budget totals, trait costs, limb mount counts, and Power Draw values in `assets/data/balance.json` are still the placeholder figures `creature.md` §9 flags as pending a dedicated prototyping pass.
- Party slot costs per Size class are likewise provisional (`combat.md` §2.1, §9) and want final tuning against real encounters.
